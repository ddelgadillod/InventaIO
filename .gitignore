# Data files (too large for git)
data/raw/
data/processed/
data/raw_real/
data/processed_real/
*.csv
*.parquet

# Excepcion: estos 3 CSV de etl_real/ NO son datos generados -- son
# decisiones de negocio versionables (clasificacion de categoria revisada
# producto por producto, correcciones puntuales, exclusiones), la regla
# *.csv de arriba los descartaria en silencio si no se listan aqui.
!etl_real/categoria_manual_override.csv
!etl_real/correccion_heuristica.csv
!etl_real/productos_excluidos.csv

# Environment
.env
venv/
__pycache__/
*.pyc

# Docker
pgdata/

# IDE
.vscode/
.idea/

.favorita-grocery-sales-forecasting.zip
etl/.env
*.zip
etl/.venv
etl/.venv/
api/.venv/
ml_service/.venv/
frontend/node_modules/

# Datos generados por pytest-cov (no confundir con .coveragerc, que sí se versiona)
.coverage
